# kill-prop backend runtime dependencies.
#
# These are the packages required to run the FastAPI API server and the
# 6-stage analysis pipeline (ingest → normalize → cluster → consensus →
# scoring → presentation). LLM-based claim extraction is OPTIONAL and lives
# in requirements-llm.txt — only install it if you want local TinyLlama
# inference (it pulls in llama-cpp-python, which requires a C++ toolchain).
#
# Versions are pinned to the range known to pass the full test suite
# (222 tests). See requirements.lock for a fully reproducible lockfile.

fastapi>=0.110.0,<0.140.0
uvicorn[standard]>=0.29.0,<0.50.0
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
