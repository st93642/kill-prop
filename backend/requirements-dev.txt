# Test and development dependencies for the kill-prop backend.
#
# Install with:  pip install -r backend/requirements-dev.txt
# (this automatically pulls in requirements.txt as well).

-r requirements.txt

pytest>=8.0.0,<10.0.0
httpx>=0.27.0,<1.0.0
